diff --git a/core/migrations/0004_sectionmodel_case_law_sectionmodel_directive.py b/core/migrations/0004_sectionmodel_case_law_sectionmodel_directive.py
new file mode 100644
index 0000000..bf3698a
--- /dev/null
+++ b/core/migrations/0004_sectionmodel_case_law_sectionmodel_directive.py
@@ -0,0 +1,21 @@
+from django.db import migrations, models
+
+
+class Migration(migrations.Migration):
+
+    dependencies = [
+        ('core', '0003_searchhistorymodel'),
+    ]
+
+    operations = [
+        migrations.AddField(
+            model_name='sectionmodel',
+            name='case_law',
+            field=models.TextField(blank=True, null=True),
+        ),
+        migrations.AddField(
+            model_name='sectionmodel',
+            name='directive',
+            field=models.TextField(blank=True, null=True),
+        ),
+    ]
diff --git a/models/section_models.py b/models/section_models.py
index e94ee5d..043da68 100644
--- a/models/section_models.py
+++ b/models/section_models.py
@@ -9,6 +9,8 @@ class SectionModel(BaseModel):
     offense = models.TextField()
     penalty = models.TextField()
     note = models.TextField(blank=True, null=True)
+    case_law = models.TextField(blank=True, null=True)
+    directive = models.TextField(blank=True, null=True)
 
     class Meta:
         app_label = "core"
diff --git a/static/website/css/styles.css b/static/website/css/styles.css
index efd0b77..03b651a 100644
--- a/static/website/css/styles.css
+++ b/static/website/css/styles.css
@@ -796,6 +796,12 @@ body {
   border-color: var(--navy-light);
 }
 
+.detail-btn.active {
+  background: var(--navy);
+  color: #fff;
+  border-color: var(--navy);
+}
+
 .add-bm-btn {
   background: var(--navy);
   color: #fff;
diff --git a/templates/dashboard/forms/section_form.html b/templates/dashboard/forms/section_form.html
index 73d2c9e..c707220 100644
--- a/templates/dashboard/forms/section_form.html
+++ b/templates/dashboard/forms/section_form.html
@@ -23,10 +23,50 @@
                 <div class="editor rich-text-editor" data-target="penalty">{{ section.penalty|safe }}</div>
             </div>
             <div class="mb-3">
-                <label class="form-label">{% translate 'note' request.session.language %}</label>
-                <textarea name="note" style="display:none;">{{ section.note|safe|default:'' }}</textarea>
-                <div class="editor rich-text-editor" data-target="note">{{ section.note|safe }}</div>
+                <div class="form-check form-switch">
+                    <input class="form-check-input section-toggle" type="checkbox" role="switch"
+                        name="show_note" id="show_note" data-target-block="note-block"
+                        {% if section.note %}checked{% endif %}>
+                    <label class="form-check-label" for="show_note">{% translate 'note' request.session.language %} (မှတ်စု)</label>
+                </div>
+                <div class="section-toggle-block" id="note-block" style="{% if not section.note %}display:none;{% endif %}">
+                    <textarea name="note" style="display:none;">{{ section.note|safe|default:'' }}</textarea>
+                    <div class="editor rich-text-editor" data-target="note">{{ section.note|safe }}</div>
+                </div>
+            </div>
+            <div class="mb-3">
+                <div class="form-check form-switch">
+                    <input class="form-check-input section-toggle" type="checkbox" role="switch"
+                        name="show_case_law" id="show_case_law" data-target-block="case-law-block"
+                        {% if section.case_law %}checked{% endif %}>
+                    <label class="form-check-label" for="show_case_law">{% translate 'case_law' request.session.language %} (စီရင်ထုံး)</label>
+                </div>
+                <div class="section-toggle-block" id="case-law-block" style="{% if not section.case_law %}display:none;{% endif %}">
+                    <textarea name="case_law" style="display:none;">{{ section.case_law|safe|default:'' }}</textarea>
+                    <div class="editor rich-text-editor" data-target="case_law">{{ section.case_law|safe }}</div>
+                </div>
+            </div>
+            <div class="mb-3">
+                <div class="form-check form-switch">
+                    <input class="form-check-input section-toggle" type="checkbox" role="switch"
+                        name="show_directive" id="show_directive" data-target-block="directive-block"
+                        {% if section.directive %}checked{% endif %}>
+                    <label class="form-check-label" for="show_directive">{% translate 'directive' request.session.language %} (ညွှန်ကြားချက်)</label>
+                </div>
+                <div class="section-toggle-block" id="directive-block" style="{% if not section.directive %}display:none;{% endif %}">
+                    <textarea name="directive" style="display:none;">{{ section.directive|safe|default:'' }}</textarea>
+                    <div class="editor rich-text-editor" data-target="directive">{{ section.directive|safe }}</div>
+                </div>
             </div>
         </div>
     </div>
 </div>
+
+<script>
+    document.querySelectorAll(".section-toggle").forEach(function (checkbox) {
+        checkbox.addEventListener("change", function () {
+            const block = document.getElementById(this.getAttribute("data-target-block"));
+            if (block) block.style.display = this.checked ? "" : "none";
+        });
+    });
+</script>
diff --git a/templates/website/section_detail.html b/templates/website/section_detail.html
index 800c3fd..faac7fe 100644
--- a/templates/website/section_detail.html
+++ b/templates/website/section_detail.html
@@ -36,15 +36,35 @@
         </div>
       </div>
       {% endif %}
+      {% if section.note or section.case_law or section.directive %}
       <div class="detail-btn-row">
-        <button class="detail-btn" type="button"><i class="bi bi-journal-text"></i> မှတ်စု</button>
-        <button class="detail-btn" type="button"><i class="bi bi-bank"></i> စီရင်ထုံး</button>
-        <button class="detail-btn" type="button"><i class="bi bi-file-earmark-text"></i> ညွှန်ကြားချက်</button>
+        {% if section.note %}
+        <button class="detail-btn" type="button" onclick="toggleDetailSection('note')"><i class="bi bi-journal-text"></i> မှတ်စု</button>
+        {% endif %}
+        {% if section.case_law %}
+        <button class="detail-btn" type="button" onclick="toggleDetailSection('case-law')"><i class="bi bi-bank"></i> စီရင်ထုံး</button>
+        {% endif %}
+        {% if section.directive %}
+        <button class="detail-btn" type="button" onclick="toggleDetailSection('directive')"><i class="bi bi-file-earmark-text"></i> ညွှန်ကြားချက်</button>
+        {% endif %}
       </div>
+      {% endif %}
 
-      <div class="detail-text">
+      {% if section.note %}
+      <div class="detail-text extra-section" id="section-note" style="display:none;">
         {{section.note|safe}}
       </div>
+      {% endif %}
+      {% if section.case_law %}
+      <div class="detail-text extra-section" id="section-case-law" style="display:none;">
+        {{section.case_law|safe}}
+      </div>
+      {% endif %}
+      {% if section.directive %}
+      <div class="detail-text extra-section" id="section-directive" style="display:none;">
+        {{section.directive|safe}}
+      </div>
+      {% endif %}
       <!-- <div class="related-label">Related Offences</div>
       <div class="tag-row">
         <span class="tag" onclick="goTo('section-detail')">Section 300</span>
@@ -78,13 +98,36 @@
     chapter_number: "{{ section.chapter.chapter_number|escapejs }}",
     offense: "{{ section.offense|escapejs }}",
     penalty: "{{ section.penalty|escapejs }}",
-    note: "{{ section.note|default:''|escapejs }}"
+    note: "{{ section.note|default:''|escapejs }}",
+    case_law: "{{ section.case_law|default:''|escapejs }}",
+    directive: "{{ section.directive|default:''|escapejs }}"
   };
 
   document.addEventListener("DOMContentLoaded", function () {
     checkOfflineDownloadState();
   });
 
+  function toggleDetailSection(key) {
+    const block = document.getElementById("section-" + key);
+    const btn = document.querySelector('.detail-btn[onclick="toggleDetailSection(\'' + key + '\')"]');
+    if (!block) return;
+
+    const isOpen = block.style.display !== "none";
+
+    // close all extra sections first
+    document.querySelectorAll(".extra-section").forEach(function (el) {
+      el.style.display = "none";
+    });
+    document.querySelectorAll(".detail-btn").forEach(function (b) {
+      b.classList.remove("active");
+    });
+
+    if (!isOpen) {
+      block.style.display = "";
+      if (btn) btn.classList.add("active");
+    }
+  }
+
   function checkOfflineDownloadState() {
     const btn = document.getElementById("btnOfflineDownload");
     if (!btn) return;
diff --git a/views/dashboard/section_views.py b/views/dashboard/section_views.py
index 87ea474..d997993 100644
--- a/views/dashboard/section_views.py
+++ b/views/dashboard/section_views.py
@@ -18,8 +18,10 @@ def section_create(request, chapter_id):
         title = request.POST.get("title")
         offense = request.POST.get("offense")
         penalty = request.POST.get("penalty")
-        note = request.POST.get("note")
-        SectionModel.objects.create(chapter=chapter, section_number=section_number, title=title, offense=offense, penalty=penalty, note=note)
+        note = request.POST.get("note") if request.POST.get("show_note") else None
+        case_law = request.POST.get("case_law") if request.POST.get("show_case_law") else None
+        directive = request.POST.get("directive") if request.POST.get("show_directive") else None
+        SectionModel.objects.create(chapter=chapter, section_number=section_number, title=title, offense=offense, penalty=penalty, note=note, case_law=case_law, directive=directive)
         messages.success(request, CREATE)
         return redirect("section_list", chapter_id=chapter.id)
     return redirect("section_list", chapter_id=chapter.id)
@@ -33,7 +35,9 @@ def section_update(request, chapter_id, pk):
         section.title = request.POST.get("title")
         section.offense = request.POST.get("offense")
         section.penalty = request.POST.get("penalty")
-        section.note = request.POST.get("note")
+        section.note = request.POST.get("note") if request.POST.get("show_note") else None
+        section.case_law = request.POST.get("case_law") if request.POST.get("show_case_law") else None
+        section.directive = request.POST.get("directive") if request.POST.get("show_directive") else None
         section.save()
         messages.success(request, UPDATE)
         return redirect("section_list", chapter_id=chapter.id)
